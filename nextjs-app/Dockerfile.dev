FROM node:20-alpine

RUN apk add --no-cache curl

WORKDIR /app
COPY package*.json ./
RUN npm ci || npm install
# Copying the whole app is skipped for dev; we mount it via volumes.
EXPOSE 3000
# CMD provided by docker-compose
